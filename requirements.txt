# ONZE runtime + dev dependencies (Python 3.12).
# Pinned to recent, mutually compatible versions. Keep in sync with pyproject.toml.

# --- data ---
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
duckdb==1.1.3
requests==2.32.3
pyyaml==6.0.2

# --- modelling ---
scipy==1.14.1
statsmodels==0.14.4

# --- api ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- tests ---
pytest==8.3.4
hypothesis==6.122.3
